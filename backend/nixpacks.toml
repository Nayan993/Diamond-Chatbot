[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port $PORT"
```

## Step 2: Update Your Railway Settings

1. Go to your Railway project settings
2. Click on **"Settings"** tab
3. Under **"Build"** section:
   - Set **Root Directory** to: `backend`
   - Or if files are in root, leave it as `/`
4. Under **"Deploy"** section:
   - Set **Start Command** to: `uvicorn app:app --host 0.0.0.0 --port $PORT`

## Step 3: Make Sure Environment Variables are Set

In Railway Settings → Variables:
```
GEMINI_API_KEY=AIzaSyCktBYV9tTZdnXzGzGDR4q_Ux8OTm8fTtk
GEMINI_MODEL=gemini-2.5-flash
PORT=8000